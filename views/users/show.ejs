<!DOCTYPE html>
<html lang="jp">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRUD</title>
  <link href="../public/style.css" rel="stylesheet" />
</head>

<body>
  <div class="c-container">
    <h1 class="c-title-01">詳細ページ</h1>
    <p class="mb-5 text-xl">
      ID：<%= user.id %>
    </p>
    <p class="mb-5 text-xl">
      ユーザー名：<%= user.name %>
    </p>
    <div class="mb-20">
      <a class="c-link" href="/users">一覧ページへ</a>
      <a class="c-link" href="/users/<%= user.id %>/edit">編集画面へ</a>
    </div>
    <a class="c-link mx-auto" href="/">TOP</a>
    <form action="/api/users/<%= user.id %>?_method=DELETE" class="text-center" method="POST">
      <button class="c-button" type="submit">
        削除する
      </button>
    </form>
  </div>
</body>

</html>